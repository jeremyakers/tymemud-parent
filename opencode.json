{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "servers": {
      "tymemud-builder": {
        "type": "local",
        "name": "TymeMUD Builder Gateway",
        "description": "MCP gateway for TymeMUD world building via BuilderPort protocol v1",
        "command": ["python3", "-m", "llm_gateway.server"],
        "environment": {
          "PYTHONPATH": "."
        },
        "enabled": true
      }
    }
  }
}
