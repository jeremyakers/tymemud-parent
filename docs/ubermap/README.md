## Ubermap (overland grid) – investigation index

This folder consolidates **all Ubermap-related information** I could recover from:

- dotproject exports (`dotproject_files/*.ods`, converted to `tmp/dotproject_csv/*.csv`)
- in-game vboards stored in MySQL (`vboard_messages`, etc.)
- SQL mudmail stored in MySQL (`mail_headers`, `mail_messages`, etc.)
- the actual world files (`MM32/lib/world/wld/*.wld`, `MM32/lib/world/zon/*.zon`)
- the design image (`Building_Design_Notes/ubermap.jpg`)

### Quick pointers

- **Design artifact**: `design_from_ubermap_jpg.md`
- **Physical scope rules (source of truth)**: `physical_scope_policy.md`
- **Workflow policy (how we validate + when to revert)**: `workflow_policy.md`
- **Dotproject task extraction**: `dotproject_extract.md`
- **Vboards extraction summary**: `vboards_extract.md`
- **Mudmail extraction summary**: `mudmail_extract.md`
- **World-state inventory (what exists, what’s placeholder, connectivity stubs)**: `world_inventory.md`
- **Plan to finish**: `finish_plan.md`

